{% extends 'base.html.twig' %}
{% block body %}

    <main>
        {% for article in articles %}
        <div class="py-5 mt-5">
            <div class="card">
                <h5 class="card-header">{{ article.title }}</h5>
                <div class="card-body">
                    <h5 class="card-title">{{ article.title }}</h5>
                    <p class="card-text">{{ article.text }}</p>
                        <a href="{{ path('detail-article', {'id': article.id }) }}" class="btn btn-primary">voir détail</a>
                </div>
            </div>
        </div>
        {% endfor %}


    </main>



{% endblock %}

